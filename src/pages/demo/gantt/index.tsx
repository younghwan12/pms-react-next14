import { GanttContainer } from "@/features/demo/gantt/components";
import AppWrapper from "@/layout/AppWrapper";

const GanttPage = () => {
    return (
        <AppWrapper>
            <GanttContainer />
        </AppWrapper>
    );
};

export default GanttPage;

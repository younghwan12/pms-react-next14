import { CrudContainer } from "@/features/demo/crud/components";
import AppWrapper from "@/layout/AppWrapper";
import React from "react";

const CrudPage = () => {
    return (
        <AppWrapper>
            <CrudContainer />
        </AppWrapper>
    );
};

export default CrudPage;

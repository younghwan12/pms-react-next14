import { DemoContainer } from "@/features/demo/components";
import AppWrapper from "@/layout/AppWrapper";
import React from "react";

const DemoPage = () => {
    return (
        <AppWrapper>
            <DemoContainer />
        </AppWrapper>
    );
};

export default DemoPage;

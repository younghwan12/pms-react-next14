import { ReactFlowContainer } from "@/features/demo/reactflow/components";
import AppWrapper from "@/layout/AppWrapper";
import React from "react";

const reactFlowPage = () => {
    return (
        <AppWrapper>
            <ReactFlowContainer />
        </AppWrapper>
    );
};

export default reactFlowPage;

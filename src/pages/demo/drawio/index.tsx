import { DrawioContainer } from "@/features/demo/drawio/components";
import AppWrapper from "@/layout/AppWrapper";

const CrudPage = () => {
    return (
        <AppWrapper>
            <DrawioContainer />
        </AppWrapper>
    );
};

export default CrudPage;
